import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ajspeller.flashlight',
  appName: 'flashlight',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
